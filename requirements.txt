# Explore Studio — Runtime Dependencies
# (currently none — project is in Phase 0 foundation)

# Development dependencies are declared in pyproject.toml [project.optional-dependencies] dev
# Install with: pip install -e ".[dev]"
