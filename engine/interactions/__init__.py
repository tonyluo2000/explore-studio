"""Explore Studio engine — interaction dispatch.

Detects when interactions should fire between entities and invokes the
appropriate handler. Owns interaction conditions, dispatch ordering,
and interaction state management.

Ownership: Engine team.
"""
