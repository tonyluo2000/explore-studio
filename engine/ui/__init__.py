"""Explore Studio engine — user interface primitives.

Owns UI elements rendered above the world: menus, dialogs, HUD elements,
text displays. UI is always rendered on top of the world layer and never
becomes part of world state.

Ownership: Engine team.
"""
